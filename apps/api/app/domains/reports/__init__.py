"""Reports domain (slice 8): dashboard KPIs + financial reports + reconciliation.

Every figure in this package is derived from POSTED journal entries only —
the ledger is the source of truth. See `service.py` for each report's
documented method and its reconciliation invariant.
"""
